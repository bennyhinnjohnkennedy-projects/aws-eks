apiVersion: v1
kind: Service
metadata:
  name: llm-stack
  namespace: llm-stack
spec:
  type: ClusterIP
  selector:
    app: llm-stack
  ports:
    - name: api
      port: 8000
      targetPort: 8000
      protocol: TCP
      